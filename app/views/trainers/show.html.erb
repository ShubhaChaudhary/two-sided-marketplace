    <div class="panel panel-default show-panel">
  <div class="row">
    <%# This line will get replace with categories from Sport Table in data  %>
    <%# <% @coach.each do |item, detail| %> 
    <%# <% @trainers.each do |trainer|%> 
      <div class="col-md-4">
        <div class="img-thumbnails thumbnail">
        <%= image_tag(@trainer.avatar.url, class: "media-object") %> 
          <%# <%= image_tag(detail[:image], alt: "Profile image") %>                 
        </div>  
        <div class="caption  text-center">
          <h3><%= @trainer.experience  %></h3>
          <p class="lead text-center">*****</p>
        </div>      
      </div>    

      <div class="col-md-8">
        <h3 class="media-heading"><%= @trainer.first_name %> &nbsp; <%= @trainer.last_name %></h3>


        <%# <h5 class="p-2"><%= detail[:city] %></h5> 
        <p class="p-2"><%= @trainer.bio %></p>
        <p class="text-center bg bg-danger py-5">Au$50/hr</p>
        <%= link_to('Back', trainers_path, class: "btn btn-info" ) %>
        <%= link_to('Add to Cart', 
                     stripeit_path, 
                     class: "btn btn-warning pull-right", 
                     id: 'add-to-cart' ) %>
        <%# <%= link_to('Book Now', 
                     stripeit_path, 
                     class: "btn btn-active pull-right",
                     id: 'book-now' )  %>
      </div>    
  
  </div>
</div>

<%= link_to 'Edit', edit_trainer_path(@trainer) %> |
<%= link_to 'Back', trainers_path %> | 
<%= link_to('Delete', @trainer, method: :delete, data: {confirm:  "Are you sure?"}) %>
